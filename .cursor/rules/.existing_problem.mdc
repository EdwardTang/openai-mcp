# Claude Code Python Edition MCP 服务器问题文档

## 核心问题

在尝试使用 Claude Code Python Edition MCP 服务器时，遇到了以下关键问题：

1. **FastMCP 兼容性错误**：`'FastMCP' object has no attribute 'app'`
   - 服务器无法启动，因为 FastMCP 类的 API 可能发生了变化
   - 在 `claude_code/mcp_server.py` 文件的 `main()` 函数中尝试访问 `server.app` 属性时失败

2. **消息解析错误**：`Failed to parse message: 8 validation errors for JSONRPCMessage`
   - 当尝试向服务器发送消息时，消息格式验证失败
   - 服务器期望 JSON-RPC 2.0 格式的消息，而不是简单的 JSON 格式

3. **路由处理错误**：`TypeError: 'NoneType' object is not callable`
   - 在请求处理过程中，Starlette 路由尝试调用一个返回 `None` 的处理函数
   - 这可能是由于路由配置不正确或处理函数未正确实现

4. **官方客户端不可用**：
   - 尝试使用官方 `mcp-client` 命令也失败
   - 错误表明 FastMCP 库版本与代码不兼容

## 尝试但失败的方法

1. 使用 curl 发送请求：
   ```bash
   curl -X POST "http://127.0.0.1:8888/messages?session_id=2d8377ccd5414f009649e41db0a6d00f" \
     -H "Content-Type: application/json" \
     -d '{"id":"msg_test123","type":"message","role":"user","content":"什么是 MCP?"}'
   ```
   结果：`Could not parse message`

2. 使用官方客户端：
   ```bash
   python -m claude_code.claude mcp-client claude_code/mcp_server.py
   ```
   结果：`AttributeError: 'FastMCP' object has no attribute 'app'`

## 可能的解决方案

1. **修复服务器代码**：
   - 编辑 `claude_code/mcp_server.py` 文件
   - 修改 `main()` 函数中的问题代码，使用正确的 FastMCP API

2. **降级 FastMCP 库**：
   - 安装与代码兼容的 FastMCP 版本
   - `pip install fastmcp==X.Y.Z`（X.Y.Z 为兼容版本）

3. **使用替代 MCP 实现**：
   - 如果可能，尝试使用 `modal_mcp_server.py`
   - 或其他兼容的 MCP 服务器实现

4. **修复 JSON-RPC 格式**：
   - 使用正确的 JSON-RPC 2.0 格式发送消息
   - 包含必需的 `jsonrpc`、`method`、`params` 和 `id` 字段

## 环境信息

- Python 版本: 3.10
- FastMCP 版本: 需确认
- Claude Code Python Edition 版本: 0.1.0
- 运行环境: Linux 系统 (Ubuntu)

## 进一步调查建议

1. 检查 FastMCP 库的文档和版本变更记录
2. 查看 Claude Code 代码库是否有相关问题报告或更新
3. 检查 `claude_code/mcp_server.py` 的具体实现，特别是 `initialize_server()` 函数
4. 尝试使用调试模式启动服务器以获取更详细的错误信息 